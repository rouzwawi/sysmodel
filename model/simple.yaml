simple:
  system: sysmodel
  visible: true

  image: spotify:80/something

  description: |
    some arbitrary text here
    that can span multiple lines
    and: look like yaml
      foo: 111

  dependencies:
    db:
      uri: sysmodel:db

  facts:
    foo: bar
    bax: quax
    structured:
      data: in here
      and: 1928
      with: true
