system: sysmodel
visible: true

description: |
  some arbitrary text here
  that can span multiple lines
  and: look like yaml
    foo: 111

dependencies:
  db:
    uri: sysmodel:db

facts:
  foo: bar
  bax: quax
  structured:
    data: in here
    and: 1928
    with: true
